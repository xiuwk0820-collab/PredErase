# Core
torch>=2.1.0
torchvision>=0.16.0
diffusers>=0.35.0
transformers>=4.40.0
accelerate>=0.27.0
safetensors>=0.4.0
huggingface_hub>=0.23.0

# Images / numerics
Pillow>=10.0.0
numpy>=1.24.0
scipy>=1.11.0
opencv-python-headless>=4.8.0
tqdm>=4.66.0
PyYAML>=6.0
einops>=0.7.0

# Metrics (OmniEraser / SmartEraser protocols)
lpips>=0.1.4
timm>=0.9.0

# Optional: ReMOVE metric needs segment-anything + SAM ViT-H weights
# segment-anything @ git+https://github.com/facebookresearch/segment-anything.git
